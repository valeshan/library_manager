extends layout

block content
  include includes/patron_menus/patron_table
    each patron in patrons
      tr
        td
          a(href='all_patrons/' +patron.patron_id)=patron.first_name+' '+patron.last_name
        td=patron.address
        td=patron.email
        td=patron.library_id
        td=patron.zip_code
